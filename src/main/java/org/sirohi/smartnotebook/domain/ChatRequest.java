package org.sirohi.smartnotebook.domain;

/**
 * Domain-level chat request. Provider-agnostic — never import SDK types here.
 */
public record ChatRequest(
// TODO: define fields — systemPrompt, userMessage, context, parameters
) {
}
