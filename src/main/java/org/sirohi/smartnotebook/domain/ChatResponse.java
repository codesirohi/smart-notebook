package org.sirohi.smartnotebook.domain;

/**
 * Domain-level chat response. Provider-agnostic — never import SDK types here.
 */
public record ChatResponse(
// TODO: define fields — answer, citations, modelUsed, latencyMs
) {
}
