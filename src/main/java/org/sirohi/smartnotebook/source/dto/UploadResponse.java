package org.sirohi.smartnotebook.source.dto;

/**
 * Response DTO for document upload (returns 202 Accepted with document ID).
 */
public record UploadResponse(
// TODO: define fields — documentId, status, message
) {
}
